import classes from './Calculator.module.css';

const UserProfile = () => {
  return (
    <main className={classes.profile}>
      <h2>My Calculator</h2>
    </main>
  );
};

export default UserProfile;
